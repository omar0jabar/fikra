{% extends get_admin_template('base_list_field', admin.code) %}

{% block field %}
   {%- if value is not empty -%}
      {% set month = value|date('F') %}
      {{ value|date('d') }}
      {{ month|trans }}
      {{ value|date('Y') }}
      {{ value|date('H:i') }}
   {%- endif -%}
{% endblock %}